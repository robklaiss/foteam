<?php
require_once 'includes/config.php';
require_once 'includes/functions.php';

// Redirect to login if not logged in
if (!is_logged_in()) {
    $_SESSION['redirect_after_login'] = 'orders.php';
    set_flash_message('Debe iniciar sesión para ver sus pedidos', 'warning');
    redirect('login.php');
}

// Get order ID from URL
$order_id = isset($_GET['id']) ? (int)$_GET['id'] : 0;

if ($order_id <= 0) {
    set_flash_message('ID de pedido inválido', 'danger');
    redirect('orders.php');
}

// Get order details
$order = get_order_details($order_id, $_SESSION['user_id']);

if (!$order) {
    set_flash_message('Pedido no encontrado', 'danger');
    redirect('orders.php');
}

// Include header
include 'includes/header.php';
?>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-success text-white">
                    <h3 class="mb-0">¡Pedido Confirmado!</h3>
                </div>
                <div class="card-body">
                    <div class="text-center mb-4">
                        <i class="bi bi-check-circle-fill text-success" style="font-size: 4rem;"></i>
                        <h4 class="mt-3">Gracias por su compra</h4>
                        <p>Su pedido ha sido procesado correctamente.</p>
                    </div>
                    
                    <div class="card mb-4">
                        <div class="card-header">
                            <h5 class="mb-0">Detalles del Pedido</h5>
                        </div>
                        <div class="card-body">
                            <p><strong>Número de Pedido:</strong> #<?php echo $order_id; ?></p>
                            <p><strong>Fecha:</strong> <?php echo date('d/m/Y H:i', strtotime($order['order_date'])); ?></p>
                            <p><strong>Total:</strong> $<?php echo number_format($order['total_amount'], 2); ?></p>
                            <p><strong>Estado:</strong> 
                                <?php if ($order['status'] === 'completed'): ?>
                                    <span class="badge bg-success">Completado</span>
                                <?php elseif ($order['status'] === 'cancelled'): ?>
                                    <span class="badge bg-danger">Cancelado</span>
                                <?php else: ?>
                                    <span class="badge bg-warning text-dark">Pendiente</span>
                                <?php endif; ?>
                            </p>
                        </div>
                    </div>
                    
                    <div class="card mb-4">
                        <div class="card-header">
                            <h5 class="mb-0">Información de Contacto</h5>
                        </div>
                        <div class="card-body">
                            <p><strong>Nombre:</strong> <?php echo h($order['customer_name']); ?></p>
                            <p><strong>Correo Electrónico:</strong> <?php echo h($order['customer_email']); ?></p>
                            <p><strong>Teléfono:</strong> <?php echo h($order['customer_phone']); ?></p>
                        </div>
                    </div>
                    
                    <div class="card">
                        <div class="card-header">
                            <h5 class="mb-0">Fotos Compradas (<?php echo count($order['items']); ?>)</h5>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <?php foreach ($order['items'] as $item): ?>
                                <div class="col-md-4 mb-3">
                                    <div class="card">
                                        <img src="<?php echo h($item['thumbnail_url']); ?>" class="card-img-top" alt="Foto de Maratón">
                                        <div class="card-body p-2 text-center">
                                            <a href="download.php?order_id=<?php echo $order_id; ?>&photo_id=<?php echo $item['image_id']; ?>" class="btn btn-sm btn-primary">
                                                <i class="bi bi-download"></i> Descargar
                                            </a>
                                        </div>
                                    </div>
                                </div>
                                <?php endforeach; ?>
                            </div>
                        </div>
                    </div>
                    
                    <div class="text-center mt-4">
                        <a href="orders.php" class="btn btn-primary">Ver Todos Mis Pedidos</a>
                        <a href="index.php" class="btn btn-outline-primary ms-2">Volver al Inicio</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<?php include 'includes/footer.php'; ?>
